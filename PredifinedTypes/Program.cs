using System;

namespace PredifinedTypes
{
    class Program
    {
        static void Main(string[] args)
        {
            string text = "true";
            bool valueAsBool = bool.Parse(text);
        }
    }
}
